services:
  - type: web
    runtime: node
    name: copypasta-relay
    plan: free
    region: oregon
    rootDir: relay-server
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
